<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para o diálogo de adicionar ou editar um local. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Título do diálogo. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Adicionar Local"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/dark_green"
        android:layout_marginBottom="20dp" />

    <!-- Título do campo Nome do local. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:text="Nome do local"
        android:textColor="@color/dark_green"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- EditText para o nome do local. -->
    <EditText
        android:id="@+id/etPlaceName"
        style="@style/Edit_Text_Add"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"
        android:hint="Digite o nome do local" />

    <!-- Título do campo Latitude. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:text="Latitude"
        android:textColor="@color/dark_green"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- EditText para a latitude. -->
    <EditText
        android:id="@+id/etLatitude"
        style="@style/Edit_Text_Add"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"
        android:hint="-23.550520"
        android:inputType="numberDecimal|numberSigned" />

    <!-- Título do campo Longitude. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5dp"
        android:text="Longitude"
        android:textColor="@color/dark_green"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- EditText para a longitude. -->
    <EditText
        android:id="@+id/etLongitude"
        style="@style/Edit_Text_Add"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="24dp"
        android:hint="-46.633309"
        android:inputType="numberDecimal|numberSigned" />

    <!-- Contêiner para os botões de ação (Cancelar e Salvar). -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <!-- Botão para cancelar a operação. -->
        <Button
            android:id="@+id/btnCancel"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="Cancelar"
            android:textColor="@color/dark_green" />

        <!-- Botão para salvar o local. -->
        <Button
            android:id="@+id/btnSave"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/dark_green"
            android:text="Salvar"
            android:textColor="@android:color/white" />

    </LinearLayout>

</LinearLayout> 